doctype html
html
  head
    title= title
    
    //import stylesheets
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(rel='stylesheet', href='/stylesheets/animate.css')


    // Let browser know website is optimized for mobile
    meta(name='viewport', content='width=device-width, initial-scale=1.0')

    //import javascripts
    //too many scripts.... rip readability...
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/gsap/latest/TweenMax.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/gsap/latest/plugins/ScrollToPlugin.min.js')
    script(language='Javascript', type='text/javascript', src='/javascripts/smoothPageScroll.js')
    script(language='Javascript', type='text/javascript', src='/javascripts/script.js')
    script.
      $(document).ready(function() {
        $('a[href^="#"]').click(function() {
            var target = $(this.hash);
            if (target.length == 0) target = $('a[name="' + this.hash.substr(1) + '"]');
            if (target.length == 0) target = $('html');
            $('html, body').animate({ scrollTop: target.offset().top-50  }, 1000);
            return false;
        });
      });
    script.
      $(window).load(function() {
      // Animate loader off screen
      $(".se-pre-con").fadeOut("slow");;
      });



    script(language='Javascript', type='text/javascript', src='/javascripts/wow.js')
    script(type='text/javascript', src='/javascripts/materialize.js')
    script.
      new WOW().init();

    //stuff
    

  body
    .se-pre-con
    include header
    .row.cow1#top
    .face-thing
      .wow.fadeInLeft
        img.img-circle(src="/images/profile.png" style="width: 12%; height: 12%")
    .face-words
      .wow.fadeInRight
        h1.featurette-heading
          font(color="white") 
            |Hi! My name is Harrison.
    
    .wow.fadeIn#aboutme
      .container
        .row.whoami
          h1 A Little About Me
        .border
      .container
        .row.aboutme
          p.lead.text-muted
            |I'm currently a sophomore in computer engineering
            |at the University of Illinois 
            |Urbana-Champaign, and I love to code! I also enjoy playing badminton and 
            |piano, and in my spare time, I play Hearthstone, League of Legends, and 
            | other games too. While not at school, I live in Palo Alto, California.
    #intro(data-speed='10', data-type='background')
      .row
        .intro-unit

    .wow.fadeIn
      .container
        .row.whoami
          h1 Past Projects
        .border
      .container
        .row.aboutme
          p.lead.text-muted
            |Yes.

      #intro2(data-speed='10', data-type='background')
        .row
          .intro-unit

    .container
      .row.featurette
        .col-md-7.col-md-push-5
          h1.featurette-heading
            | The second section. 
            span.text-muted Pretty cool.
          p.lead
            | It's really cool, I swear. 
            | This one's even gonna have a picture in it probably.
      hr.featurette-divider
    .container
      .row.featurette
        .col-md-7
          h1.featurette-heading
            | And lastly, this one. 
            span.text-muted Checkmate.
          p.lead
            | Yeah, that's right, the page scrolls. I'm just typing random words here
            | to make it look like there's actually some content but it's actually just
            | some garbage filler text
      hr.featurette-divider
